# Nothing is printed because nothing is called. Return is proc object. Once error is fixed (once anonymous proc is called), the puts message will be printed and return will be nil because of puts. 
#
#
