def valid_number?(number_string)
  number_string.to_i.to_s == number_string && number_string.to_i != 0
end

# This externally-provided method verifies that the user input string contains an integer that is not 0.

# Write using methods for all repetitive code!
